import { memo } from 'react';

const RoomsPage = memo(function RoomsPage() {
  return (
    <div className="flex items-center justify-center min-h-[60vh]">
      <p className="text-text/50 text-lg">Страница номеров (в разработке)</p>
    </div>
  );
});

export default RoomsPage;
